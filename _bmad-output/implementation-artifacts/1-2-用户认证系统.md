# Story 1.2: 用户认证系统（登录/登出/Token刷新）

**Status:** done
**Story Key:** 1-2-用户认证系统
**Epic:** 1 - 系统初始化与用户认证
**Date:** 2026-04-14

---

## User Story

As a 系统用户,
I want 通过账号密码安全登录系统,
So that 我的操作经过身份验证，未登录不可访问任何业务页面。

---

## Acceptance Criteria

**Given** users 表已创建（含 username, password_hash, role, is_active 字段）
**When** 用户提交正确的用户名和密码到 `POST /api/v1/auth/login`
**Then** 返回 200，设置 HTTP-only Cookie（access_token 有效期 30 分钟，refresh_token 有效期 7 天）
**And** 响应体包含用户基本信息（id, username, role）

**Given** 用户未登录（无 Cookie）
**When** 访问任意 `/api/v1/*` 业务接口（除 login）
**Then** 返回 401 Unauthorized

**Given** access_token 已过期但 refresh_token 有效
**When** 调用 `POST /api/v1/auth/refresh`
**Then** 返回新的 access_token Cookie，用户无感刷新

**Given** 用户已登录
**When** 调用 `POST /api/v1/auth/logout`
**Then** 清除 access_token 和 refresh_token Cookie

**Given** 用户密码存储
**When** 查看数据库 password_hash 字段
**Then** 使用 bcrypt 哈希，非明文存储

---

## Scope

### In Scope
- 后端：User 数据模型 + users 表 Alembic 迁移
- 后端：JWT 签发/验证（access + refresh token，HTTP-only Cookie）
- 后端：bcrypt 密码哈希
- 后端：login / logout / refresh / me 四个 API 端点
- 后端：`get_current_user` 依赖注入（全局 401 保护）
- 后端：种子数据（4 个测试用户）
- 前端：LoginPage 登录表单（Ant Design）
- 前端：ProtectedRoute 路由守卫
- 前端：App.tsx 路由配置（login 路由 + 受保护根路由）
- 前端：401 拦截器（跳转到登录页）
- 前端：authStore 登录/登出状态管理

### Out of Scope（不要实现）
- RBAC 权限矩阵与角色访问控制 → Story 1.3
- 左侧菜单导航、CacheTabs、完整布局 → Story 1.4
- 通用 UI 组件库 → Story 1.5
- Story 1.2 完成后，App.tsx 中 `/` 只需一个占位页面，不需要业务导航

---

## Tasks/Subtasks

### 后端

- [x] T1: User 模型 + models/__init__.py 导入
- [x] T2: security.py 实现（create_access_token / create_refresh_token / verify_token / hash_password / verify_password）
- [x] T3: app/schemas/auth.py（LoginRequest / UserResponse / LoginResponse）
- [x] T4: app/repositories/users.py（UserRepository + get_by_username）
- [x] T5: app/services/auth.py（AuthService: login / refresh / get_user_by_token）
- [x] T6: app/routers/auth.py（login / logout / refresh / me 四个端点）
- [x] T7: app/deps.py — 实现 get_current_user
- [x] T8: app/main.py — 注册 auth router
- [x] T9: Alembic 迁移（创建 users 表）
- [x] T10: app/db/seed.py（4 个种子用户）
- [x] T11: tests/conftest.py 新增 test_user fixture
- [x] T12: tests/routers/test_auth.py（8 个测试场景）

### 前端

- [x] T13: src/types/auth.ts 完整替换
- [x] T14: src/api/auth.ts 实现
- [x] T15: src/api/client.ts 实现 401 拦截器
- [x] T16: src/stores/authStore.ts 确认现有实现满足需求
- [x] T17: src/features/auth/pages/LoginPage.tsx
- [x] T18: src/features/auth/components/ProtectedRoute.tsx
- [x] T19: src/App.tsx 路由配置

---

## 开发环境

| 变量 | 值 |
|------|-----|
| BACKEND_ROOT | `erp-backend/` |
| FRONTEND_ROOT | `erp-frontend/` |
| 后端启动 | `docker-compose up` 或 `uvicorn app.main:app --reload` |
| 前端启动 | `cd erp-frontend && npm run dev` |

---

## 已存在文件（修改，不要重建）

| 文件 | 当前状态 | Story 1.2 操作 |
|------|---------|----------------|
| `erp-backend/app/core/security.py` | 4 个函数 stub（`raise NotImplementedError`） | **实现全部 4 个函数**，新增 `create_refresh_token` |
| `erp-backend/app/deps.py` | `get_current_user` 占位 | **实现** JWT Cookie 解析 + DB 用户查询 |
| `erp-backend/app/main.py` | auth router 注释掉 | **取消注释**，注册 auth router |
| `erp-backend/app/core/config.py` | 已含 SECRET_KEY / TOKEN 配置 | **不改动**，直接使用 |
| `erp-backend/tests/conftest.py` | 基础 fixture | **新增** test_user fixture |
| `erp-frontend/src/types/auth.ts` | TokenResponse 缺 refresh_token | **重写**完整类型定义 |
| `erp-frontend/src/api/auth.ts` | 全部函数 stub | **实现**全部函数 |
| `erp-frontend/src/api/client.ts` | 401 拦截器有 TODO 注释 | **实现** 401 跳转逻辑 |
| `erp-frontend/src/stores/authStore.ts` | 基础 setUser/logout | **新增** login/loginLoading/error 状态 |
| `erp-frontend/src/App.tsx` | 纯占位，无路由 | **重写**：添加 BrowserRouter + 路由配置 |

---

## 新增文件清单

**后端（共 5 个，遵循 5-file module 规范）：**
- `erp-backend/app/models/user.py`
- `erp-backend/app/schemas/auth.py`
- `erp-backend/app/routers/auth.py`
- `erp-backend/app/services/auth.py`
- `erp-backend/app/repositories/users.py`
- `erp-backend/alembic/versions/xxxx_create_users_table.py`（autogenerate）
- `erp-backend/app/db/seed.py`（种子数据）
- `erp-backend/tests/routers/test_auth.py`

**前端（feature-based 结构）：**
- `erp-frontend/src/features/auth/pages/LoginPage.tsx`
- `erp-frontend/src/features/auth/components/ProtectedRoute.tsx`

---

## 后端技术规范

### 1. User 模型（`app/models/user.py`）

**重要**：`permissions.py` stub 中的 `Role` 枚举（ADMIN/MANAGER/OPERATOR/VIEWER）是**错误的占位值**，Story 1.3 会修正它。Story 1.2 在 User 模型中定义正确的 `UserRole` 枚举：

```python
from enum import Enum as PyEnum
from sqlalchemy import String, Boolean, Enum
from sqlalchemy.orm import Mapped, mapped_column
from app.db.base import BaseModel

class UserRole(str, PyEnum):
    ADMIN = "admin"
    PRODUCT_DEPT = "product_dept"
    BUSINESS_DEPT = "business_dept"
    FINANCE_DEPT = "finance_dept"

class User(BaseModel):
    __tablename__ = "users"

    username: Mapped[str] = mapped_column(
        String(50), unique=True, nullable=False, index=True
    )
    password_hash: Mapped[str] = mapped_column(String(255), nullable=False)
    role: Mapped[str] = mapped_column(
        Enum(UserRole), nullable=False, default=UserRole.PRODUCT_DEPT
    )
    is_active: Mapped[bool] = mapped_column(Boolean, default=True, nullable=False)
```

**注意**：`BaseModel` 已提供 `id / created_at / updated_at / deleted_at`，不要重复定义。

---

### 2. security.py 实现（完整替换）

```python
from datetime import datetime, timedelta, timezone
from typing import Any

from jose import JWTError, jwt
from passlib.context import CryptContext

from app.core.config import settings

pwd_context = CryptContext(schemes=["bcrypt"], deprecated="auto")


def create_access_token(data: dict[str, Any]) -> str:
    to_encode = data.copy()
    expire = datetime.now(timezone.utc) + timedelta(
        minutes=settings.ACCESS_TOKEN_EXPIRE_MINUTES
    )
    to_encode.update({"exp": expire, "type": "access"})
    return jwt.encode(to_encode, settings.SECRET_KEY, algorithm="HS256")


def create_refresh_token(data: dict[str, Any]) -> str:
    to_encode = data.copy()
    expire = datetime.now(timezone.utc) + timedelta(
        days=settings.REFRESH_TOKEN_EXPIRE_DAYS
    )
    to_encode.update({"exp": expire, "type": "refresh"})
    return jwt.encode(to_encode, settings.SECRET_KEY, algorithm="HS256")


def verify_token(token: str) -> dict[str, Any]:
    """解析 JWT，失败抛 JWTError（由调用方处理为 401）。"""
    return jwt.decode(token, settings.SECRET_KEY, algorithms=["HS256"])


def hash_password(password: str) -> str:
    return pwd_context.hash(password)


def verify_password(plain_password: str, hashed_password: str) -> bool:
    return pwd_context.verify(plain_password, hashed_password)
```

---

### 3. Auth Schemas（`app/schemas/auth.py`）

```python
from pydantic import BaseModel

class LoginRequest(BaseModel):
    username: str
    password: str

class UserResponse(BaseModel):
    id: int
    username: str
    role: str

    model_config = {"from_attributes": True}

class LoginResponse(BaseModel):
    user: UserResponse
    message: str = "登录成功"
```

---

### 4. UserRepository（`app/repositories/users.py`）

继承 `BaseRepository`（已在 Story 1-1 实现）：

```python
from sqlalchemy import select
from sqlalchemy.ext.asyncio import AsyncSession

from app.db.base import BaseModel  # noqa - 触发 BaseModel 注册
from app.models.user import User
from app.repositories.base_repository import BaseRepository


class UserRepository(BaseRepository[User]):
    def __init__(self, db: AsyncSession):
        super().__init__(User, db)

    async def get_by_username(self, username: str) -> User | None:
        result = await self.db.execute(
            select(User).where(User.username == username, User.deleted_at.is_(None))
        )
        return result.scalar_one_or_none()
```

---

### 5. AuthService（`app/services/auth.py`）

```python
from fastapi import HTTPException
from sqlalchemy.ext.asyncio import AsyncSession

from app.core.security import (
    create_access_token,
    create_refresh_token,
    hash_password,
    verify_password,
    verify_token,
)
from app.models.user import User
from app.repositories.users import UserRepository
from app.schemas.auth import LoginRequest, UserResponse
from jose import JWTError


class AuthService:
    def __init__(self, db: AsyncSession):
        self.user_repo = UserRepository(db)

    async def login(self, data: LoginRequest) -> tuple[User, str, str]:
        """返回 (user, access_token, refresh_token)"""
        user = await self.user_repo.get_by_username(data.username)
        if not user or not verify_password(data.password, user.password_hash):
            raise HTTPException(status_code=401, detail="用户名或密码错误")
        if not user.is_active:
            raise HTTPException(status_code=403, detail="账号已被禁用")

        payload = {"sub": str(user.id)}
        access_token = create_access_token(payload)
        refresh_token = create_refresh_token(payload)
        return user, access_token, refresh_token

    async def refresh(self, refresh_token: str) -> str:
        """验证 refresh_token，返回新 access_token。"""
        try:
            payload = verify_token(refresh_token)
        except JWTError:
            raise HTTPException(status_code=401, detail="refresh token 无效或已过期")
        if payload.get("type") != "refresh":
            raise HTTPException(status_code=401, detail="token 类型错误")

        user = await self.user_repo.get_by_id(int(payload["sub"]))
        if not user or not user.is_active:
            raise HTTPException(status_code=401, detail="用户不存在或已禁用")

        return create_access_token({"sub": str(user.id)})

    async def get_user_by_token(self, access_token: str) -> User:
        try:
            payload = verify_token(access_token)
        except JWTError:
            raise HTTPException(status_code=401, detail="access token 无效或已过期")
        if payload.get("type") != "access":
            raise HTTPException(status_code=401, detail="token 类型错误")

        user = await self.user_repo.get_by_id(int(payload["sub"]))
        if not user or not user.is_active:
            raise HTTPException(status_code=401, detail="用户不存在或已禁用")
        return user
```

---

### 6. Auth Router（`app/routers/auth.py`）

```python
from fastapi import APIRouter, Depends, Request, Response
from sqlalchemy.ext.asyncio import AsyncSession

from app.db.session import get_db
from app.deps import get_current_user
from app.models.user import User
from app.schemas.auth import LoginRequest, LoginResponse, UserResponse
from app.services.auth import AuthService

router = APIRouter(prefix="/auth", tags=["认证"])

_COOKIE_OPTS = dict(httponly=True, samesite="lax", secure=False)
ACCESS_MAX_AGE = 30 * 60       # 30 分钟，单位秒
REFRESH_MAX_AGE = 7 * 24 * 3600  # 7 天


@router.post("/login", response_model=LoginResponse)
async def login(
    data: LoginRequest,
    response: Response,
    db: AsyncSession = Depends(get_db),
):
    service = AuthService(db)
    user, access_token, refresh_token = await service.login(data)

    response.set_cookie("access_token", access_token, max_age=ACCESS_MAX_AGE, **_COOKIE_OPTS)
    response.set_cookie("refresh_token", refresh_token, max_age=REFRESH_MAX_AGE, **_COOKIE_OPTS)
    return LoginResponse(user=UserResponse.model_validate(user))


@router.post("/logout")
async def logout(response: Response):
    response.delete_cookie("access_token")
    response.delete_cookie("refresh_token")
    return {"message": "已登出"}


@router.post("/refresh")
async def refresh(
    request: Request,
    response: Response,
    db: AsyncSession = Depends(get_db),
):
    refresh_token = request.cookies.get("refresh_token")
    if not refresh_token:
        from fastapi import HTTPException
        raise HTTPException(status_code=401, detail="缺少 refresh token")

    service = AuthService(db)
    access_token = await service.refresh(refresh_token)
    response.set_cookie("access_token", access_token, max_age=ACCESS_MAX_AGE, **_COOKIE_OPTS)
    return {"message": "token 已刷新"}


@router.get("/me", response_model=UserResponse)
async def get_me(current_user: User = Depends(get_current_user)):
    return UserResponse.model_validate(current_user)
```

---

### 7. deps.py 实现（替换 get_current_user 占位）

```python
from collections.abc import AsyncGenerator

from fastapi import HTTPException, Request
from sqlalchemy.ext.asyncio import AsyncSession

from app.db.session import get_db
from app.services.auth import AuthService


async def get_current_user(
    request: Request,
    db: AsyncSession = Depends(get_db),
):
    access_token = request.cookies.get("access_token")
    if not access_token:
        raise HTTPException(status_code=401, detail="未登录")

    service = AuthService(db)
    return await service.get_user_by_token(access_token)


__all__ = ["get_db", "get_current_user"]
```

**注意**：`deps.py` 中 `Depends` 需要从 `fastapi` 导入。修正后的完整文件：

```python
from fastapi import Depends, HTTPException, Request
from sqlalchemy.ext.asyncio import AsyncSession

from app.db.session import get_db
from app.services.auth import AuthService


async def get_current_user(
    request: Request,
    db: AsyncSession = Depends(get_db),
):
    access_token = request.cookies.get("access_token")
    if not access_token:
        raise HTTPException(status_code=401, detail="未登录")
    service = AuthService(db)
    return await service.get_user_by_token(access_token)


__all__ = ["get_db", "get_current_user"]
```

---

### 8. main.py 修改（取消注释并注册 auth router）

在 `main.py` 中找到注释掉的 router 注册部分，替换为：

```python
from app.routers import auth as auth_router
app.include_router(auth_router.router, prefix="/api/v1")
```

---

### 9. Alembic 迁移

**操作步骤**（在 `erp-backend/` 目录）：

1. 确保 `app/models/__init__.py` 导入 User 模型（让 Alembic 发现）：
   ```python
   # app/models/__init__.py
   from app.models.user import User  # noqa
   ```
2. 生成迁移：`alembic revision --autogenerate -m "create_users_table"`
3. 检查生成的迁移文件，确认创建了 `users` 表，包含：
   - `id INT PRIMARY KEY AUTO_INCREMENT`
   - `username VARCHAR(50) UNIQUE NOT NULL`
   - `password_hash VARCHAR(255) NOT NULL`
   - `role ENUM('admin','product_dept','business_dept','finance_dept') NOT NULL`
   - `is_active BOOL NOT NULL DEFAULT TRUE`
   - `created_at`, `updated_at` DATETIME NOT NULL
   - `deleted_at` DATETIME NULL
4. 执行迁移：`alembic upgrade head`

---

### 10. 种子数据（`app/db/seed.py`）

```python
"""
开发/测试种子数据 — 4 个测试用户，覆盖所有角色。
用法：python -m app.db.seed（在 erp-backend/ 目录）
"""
import asyncio

from sqlalchemy.ext.asyncio import AsyncSession, create_async_engine, async_sessionmaker

from app.core.config import settings
from app.core.security import hash_password
from app.models.user import User, UserRole


SEED_USERS = [
    {"username": "admin", "password": "Admin123!", "role": UserRole.ADMIN},
    {"username": "product_user", "password": "Test123!", "role": UserRole.PRODUCT_DEPT},
    {"username": "business_user", "password": "Test123!", "role": UserRole.BUSINESS_DEPT},
    {"username": "finance_user", "password": "Test123!", "role": UserRole.FINANCE_DEPT},
]


async def seed():
    engine = create_async_engine(settings.DATABASE_URL)
    async_session = async_sessionmaker(engine, class_=AsyncSession, expire_on_commit=False)

    async with async_session() as session:
        for u in SEED_USERS:
            user = User(
                username=u["username"],
                password_hash=hash_password(u["password"]),
                role=u["role"],
                is_active=True,
            )
            session.add(user)
        await session.commit()
    await engine.dispose()
    print("种子数据写入完成")


if __name__ == "__main__":
    asyncio.run(seed())
```

---

### 11. 测试（`tests/routers/test_auth.py`）

测试用的 `conftest.py` 需新增 test_user fixture：

```python
# 在 tests/conftest.py 末尾追加
@pytest_asyncio.fixture
async def test_user(db_session):
    from app.core.security import hash_password
    from app.models.user import User, UserRole
    user = User(
        username="testuser",
        password_hash=hash_password("TestPass123!"),
        role=UserRole.PRODUCT_DEPT,
        is_active=True,
    )
    db_session.add(user)
    await db_session.commit()
    await db_session.refresh(user)
    return user
```

`tests/routers/test_auth.py` 测试用例要求：

```python
# 必须覆盖以下场景：
# 1. test_login_success — 正确凭证 → 200 + 响应含 user.id/username/role + Cookie 已设置
# 2. test_login_wrong_password — 错误密码 → 401
# 3. test_login_wrong_username — 不存在的用户名 → 401
# 4. test_logout — 先登录再登出 → 200 + Cookie 已清除
# 5. test_protected_endpoint_no_cookie — 无 Cookie 访问 /auth/me → 401
# 6. test_protected_endpoint_with_cookie — 携带有效 Cookie 访问 /auth/me → 200 + 返回当前用户
# 7. test_refresh_token — 有效 refresh_token → 200 + 返回新 access_token Cookie
# 8. test_refresh_invalid_token — 无效/过期 refresh_token → 401
```

---

## 前端技术规范

### 1. types/auth.ts（完整替换）

```typescript
export type UserRole = 'admin' | 'product_dept' | 'business_dept' | 'finance_dept'

export interface User {
  id: number
  username: string
  role: UserRole
}

export interface LoginRequest {
  username: string
  password: string
}

export interface LoginResponse {
  user: User
  message: string
}
```

---

### 2. api/auth.ts（完整实现）

```typescript
import apiClient from './client'
import type { LoginRequest, LoginResponse, User } from '../types/auth'

export const authApi = {
  login: (data: LoginRequest) =>
    apiClient.post<LoginResponse>('/auth/login', data).then((r) => r.data),

  logout: () =>
    apiClient.post<{ message: string }>('/auth/logout').then((r) => r.data),

  refresh: () =>
    apiClient.post<{ message: string }>('/auth/refresh').then((r) => r.data),

  getMe: () =>
    apiClient.get<User>('/auth/me').then((r) => r.data),
}
```

---

### 3. api/client.ts（实现 401 拦截器）

```typescript
import axios from 'axios'

const apiClient = axios.create({
  baseURL: '/api/v1',
  withCredentials: true,
  headers: { 'Content-Type': 'application/json' },
})

apiClient.interceptors.response.use(
  (response) => response,
  async (error) => {
    const status = error.response?.status
    const url = error.config?.url ?? ''

    // 401 且不是登录/刷新接口本身 → 跳转登录页
    if (status === 401 && !url.includes('/auth/login') && !url.includes('/auth/refresh')) {
      window.location.href = '/login'
    }
    return Promise.reject(error)
  }
)

export default apiClient
```

---

### 4. authStore.ts（新增 login action）

```typescript
import { create } from 'zustand'
import type { User } from '../types/auth'

interface AuthState {
  user: User | null
  isAuthenticated: boolean
  setUser: (user: User | null) => void
  logout: () => void
}

export const useAuthStore = create<AuthState>((set) => ({
  user: null,
  isAuthenticated: false,
  setUser: (user) => set({ user, isAuthenticated: user !== null }),
  logout: () => set({ user: null, isAuthenticated: false }),
}))
```

---

### 5. LoginPage.tsx（`src/features/auth/pages/LoginPage.tsx`）

```tsx
import { useState } from 'react'
import { useNavigate } from 'react-router-dom'
import { useMutation } from '@tanstack/react-query'
import { Button, Card, Form, Input, Typography, message } from 'antd'
import { authApi } from '../../../api/auth'
import { useAuthStore } from '../../../stores/authStore'

const { Title } = Typography

export default function LoginPage() {
  const navigate = useNavigate()
  const setUser = useAuthStore((s) => s.setUser)
  const [form] = Form.useForm()

  const loginMutation = useMutation({
    mutationFn: authApi.login,
    onSuccess: (data) => {
      setUser(data.user)
      navigate('/', { replace: true })
    },
    onError: () => {
      message.error('用户名或密码错误')
    },
  })

  const onFinish = (values: { username: string; password: string }) => {
    loginMutation.mutate(values)
  }

  return (
    <div
      style={{
        minHeight: '100vh',
        display: 'flex',
        alignItems: 'center',
        justifyContent: 'center',
        background: '#f0f2f5',
      }}
    >
      <Card style={{ width: 400 }}>
        <Title level={3} style={{ textAlign: 'center', marginBottom: 24 }}>
          跨境ERP系统
        </Title>
        <Form form={form} onFinish={onFinish} layout="vertical" requiredMark={false}>
          <Form.Item
            name="username"
            label="用户名"
            rules={[{ required: true, message: '请输入用户名' }]}
          >
            <Input placeholder="请输入用户名" size="large" />
          </Form.Item>
          <Form.Item
            name="password"
            label="密码"
            rules={[{ required: true, message: '请输入密码' }]}
          >
            <Input.Password placeholder="请输入密码" size="large" />
          </Form.Item>
          <Form.Item style={{ marginBottom: 0 }}>
            <Button
              type="primary"
              htmlType="submit"
              size="large"
              block
              loading={loginMutation.isPending}
              style={{ background: '#C41D2E' }}
            >
              登录
            </Button>
          </Form.Item>
        </Form>
      </Card>
    </div>
  )
}
```

---

### 6. ProtectedRoute.tsx（`src/features/auth/components/ProtectedRoute.tsx`）

```tsx
import { useEffect, useState } from 'react'
import { Navigate, Outlet } from 'react-router-dom'
import { Spin } from 'antd'
import { authApi } from '../../../api/auth'
import { useAuthStore } from '../../../stores/authStore'

export default function ProtectedRoute() {
  const { isAuthenticated, setUser } = useAuthStore()
  const [loading, setLoading] = useState(!isAuthenticated)

  useEffect(() => {
    if (!isAuthenticated) {
      authApi
        .getMe()
        .then((user) => {
          setUser(user)
        })
        .catch(() => {
          setUser(null)
        })
        .finally(() => {
          setLoading(false)
        })
    }
  }, [])

  if (loading) {
    return (
      <div style={{ display: 'flex', justifyContent: 'center', paddingTop: 200 }}>
        <Spin size="large" />
      </div>
    )
  }

  if (!isAuthenticated) {
    return <Navigate to="/login" replace />
  }

  return <Outlet />
}
```

---

### 7. App.tsx（路由配置）

Story 1.2 的 App.tsx 只需最小化路由，Story 1.4 会完整重写导航/布局：

```tsx
import { BrowserRouter, Routes, Route, Navigate } from 'react-router-dom'
import LoginPage from './features/auth/pages/LoginPage'
import ProtectedRoute from './features/auth/components/ProtectedRoute'

function HomePage() {
  return <div style={{ padding: 24 }}>系统主页（Story 1.4 将替换为完整导航）</div>
}

export default function App() {
  return (
    <BrowserRouter>
      <Routes>
        <Route path="/login" element={<LoginPage />} />
        <Route element={<ProtectedRoute />}>
          <Route path="/" element={<HomePage />} />
          <Route path="*" element={<Navigate to="/" replace />} />
        </Route>
      </Routes>
    </BrowserRouter>
  )
}
```

---

## 架构合规清单

| 规则 | 验证点 |
|------|--------|
| 5-file module | auth 模块有 model/schema/router/service/repository 五个文件 |
| BaseModel 继承 | `User(BaseModel)` ✓ |
| 软删除 | User 软删除通过 `deleted_at` 继承，查询加 `deleted_at.is_(None)` |
| Depends() 注入 | auth router 每个端点通过 `Depends(get_db)` 或 `Depends(get_current_user)` |
| 统一响应格式 | `LoginResponse` 使用自定义 schema；错误通过 `BusinessError`/`HTTPException` |
| TanStack Query | 前端通过 `useMutation` 调用 login，不直接调用 axios |
| Cookie 配置 | `httponly=True, samesite="lax"` |
| 软删除查询过滤 | `UserRepository.get_by_username` 已含 `deleted_at.is_(None)` |

---

## 前置故事智能（Story 1.1 经验）

**必须遵守，否则会踩坑：**

| 坑 | 正确做法 |
|----|----------|
| TanStack Query v5 废除了 `useQuery`/`useMutation` 的 `onSuccess`/`onError` option | 用 `mutation.isSuccess`/`mutation.isError` reactive 检测，或在 `onSuccess`/`onError` 参数传给 `mutate()` 调用 |
| `passlib[bcrypt]` 已在 `requirements.txt`，无需重新安装 | 直接导入 `from passlib.context import CryptContext` |
| `python-jose[cryptography]` 已在 `requirements.txt` | 直接导入 `from jose import jwt, JWTError` |
| `react-router-dom ^6.26.0` 已在 `package.json` | 直接使用，无需 npm install |
| AntD + Tailwind 样式冲突已在 1.1 解决（`important: '#root'`） | 不要改动 tailwind.config.ts |
| vitest 测试需要 `NODE_ENV=development` | `package.json` scripts 应已配置；若前端测试失败，检查此项 |
| `BaseRepository.get_by_id` 已实现软删除过滤 | 直接调用，不要重新实现 |
| `permissions.py` 中的 `Role` 枚举（MANAGER/OPERATOR/VIEWER）是错误占位 | **不要在 Story 1.2 中使用 permissions.py 的 Role 枚举**，使用 `app/models/user.py` 的 `UserRole` |

---

## 常见陷阱预防

| 陷阱 | 预防措施 |
|------|---------|
| Cookie 无法跨域传递 | CORS 已配置 `allow_credentials=True`，前端 Axios 已配置 `withCredentials: true`，开发环境通过 Nginx 代理无跨域问题 |
| refresh endpoint 被 401 拦截器拦截循环 | client.ts 中已排除 `/auth/refresh` 路径不触发跳转 |
| JWT 用 `sub` 存 user_id 时类型为字符串 | `payload["sub"]` 取出后需 `int(payload["sub"])` 转换 |
| Alembic autogenerate 未检测到 User 模型 | 确保 `app/models/__init__.py` 已 import User |
| 测试中直接 mock Cookie 困难 | 使用 `httpx.AsyncClient` 的 `cookies` 参数注入测试 Cookie |
| `delete_cookie` 需指定相同的 path/domain 参数才能真正清除 | FastAPI `response.delete_cookie("name")` 默认 path="/"，与设置时一致 |
| 前端 ProtectedRoute 刷新页面时短暂跳到 login | 用 `loading` state 在 `getMe()` 完成前显示 Spin，阻止渲染 Navigate |

---

## 完成标准

- [x] `POST /api/v1/auth/login` 正确凭证返回 200 + Cookie
- [x] `POST /api/v1/auth/login` 错误凭证返回 401
- [x] `GET /api/v1/auth/me` 无 Cookie 返回 401
- [x] `GET /api/v1/auth/me` 有效 Cookie 返回用户信息
- [x] `POST /api/v1/auth/refresh` 有效 refresh_token 返回新 access_token
- [x] `POST /api/v1/auth/logout` 清除 Cookie
- [x] users 表已创建（Alembic 迁移成功）
- [x] password_hash 使用 bcrypt（非明文）
- [x] 种子数据 4 个用户已写入
- [x] 后端测试 8 个场景全部通过（13/13 含 Story 1.1 遗留测试）
- [x] 前端登录页可正常提交，登录成功跳转到 `/`
- [x] 前端刷新页面不丢失登录状态（ProtectedRoute 调用 `/auth/me` 恢复状态）
- [x] 前端登出后跳转到 `/login`

---

## Dev Agent Record

### Implementation Notes

- `passlib[bcrypt]` 与 `bcrypt>=5.0.0` 不兼容（passlib 1.7.4 的 wrap-bug 检测失败）；改用 `bcrypt` 库直接调用，不依赖 passlib。
- SQLite 内存测试数据库需使用 UUID 命名共享内存模式（`file:{uuid}?mode=memory&cache=shared&uri=true`），避免多连接间数据隔离问题。
- `test_engine` fixture 设为函数级作用域，确保每个测试有独立的数据库实例，避免 UNIQUE constraint 冲突。
- Branch `story/1-2-user-auth-system` 基于 `story/1-1-project-skeleton-dev-env`（通过 rebase），因为 1-1 尚未合并到 main。
- 前端测试 `App.test.tsx` 需 mock `window.matchMedia`（jsdom 不实现），以及 mock `authApi.getMe` 避免实际网络请求。

### File List

**后端新增文件：**
- `erp-backend/app/models/user.py`
- `erp-backend/app/schemas/auth.py`
- `erp-backend/app/routers/auth.py`
- `erp-backend/app/services/auth.py`
- `erp-backend/app/repositories/users.py`
- `erp-backend/alembic/versions/0001_create_users_table.py`
- `erp-backend/app/db/seed.py`
- `erp-backend/tests/routers/__init__.py`
- `erp-backend/tests/routers/test_auth.py`

**后端修改文件：**
- `erp-backend/app/models/__init__.py` — 新增 User 导入
- `erp-backend/app/core/security.py` — 完整实现（改用 bcrypt 直调）
- `erp-backend/app/deps.py` — 实现 get_current_user
- `erp-backend/app/main.py` — 注册 auth router
- `erp-backend/tests/conftest.py` — 重写为 UUID 隔离内存 DB + test_user fixture
- `erp-backend/requirements.txt` — passlib → bcrypt
- `erp-backend/requirements-dev.txt` — 新增 aiosqlite

**前端新增文件：**
- `erp-frontend/src/features/auth/pages/LoginPage.tsx`
- `erp-frontend/src/features/auth/components/ProtectedRoute.tsx`

**前端修改文件：**
- `erp-frontend/src/types/auth.ts` — 完整重写类型定义
- `erp-frontend/src/api/auth.ts` — 实现全部 API 函数
- `erp-frontend/src/api/client.ts` — 实现 401 拦截器
- `erp-frontend/src/App.tsx` — 添加 BrowserRouter + ProtectedRoute 路由
- `erp-frontend/src/test/App.test.tsx` — 更新：mock authApi + matchMedia

### Change Log

- 2026-04-14: Story 1-2 实现完成，所有 T1–T19 任务已完成
- 2026-04-14: 后端 13/13 测试通过（8 个 auth 测试 + 5 个 Story 1.1 遗留测试）
- 2026-04-14: 前端 2/2 测试通过，TypeScript 类型检查通过，生产构建成功

### Review Findings

**Decision-Needed（需人工决策）：**
- [x] [Review][Decision] secure=False 是否应改为环境变量配置 — 已修复：改为 `secure=settings.COOKIE_SECURE`，config.py 新增 `COOKIE_SECURE: bool = False` [erp-backend/app/routers/auth.py]
- [x] [Review][Decision] 401 拦截器直接跳转 /login 还是先尝试无感刷新 — 推迟：简单跳转对 ERP 30 分钟会话可接受，静默刷新列入后续优化
- [x] [Review][Decision] 禁用账号返回 403 vs 401 — 保留 403：内部 ERP 系统，明确告知用户账号禁用体验更好

**Patch（需修复）：**
- [x] [Review][Patch] payload["sub"] 缺失或非整数导致未捕获异常 → 500 [erp-backend/app/services/auth.py]
- [x] [Review][Patch] LoginRequest 无字段长度约束，允许超长输入 [erp-backend/app/schemas/auth.py]
- [x] [Review][Patch] seed.py 无幂等性检查，重复运行抛 IntegrityError [erp-backend/app/db/seed.py]
- [x] [Review][Patch] ProtectedRoute 静默吞掉所有错误（含 5xx），无法区分未认证与服务端故障 [erp-frontend/src/features/auth/components/ProtectedRoute.tsx]
- [x] [Review][Patch] 401 拦截器跳转前未调用 authStore.logout()，store 保留过期用户状态 [erp-frontend/src/api/client.ts]

**Defer（推迟）：**
- [x] [Review][Defer] Token 无吊销机制，logout 后 token 仍有效至过期 [erp-backend/app/routers/auth.py] — deferred，需 Redis/DB 黑名单，超出本 Story 范围
- [x] [Review][Defer] Refresh 时旧 access_token 不失效，双 token 并存 [erp-backend/app/services/auth.py] — deferred，同上，需 token 黑名单基础设施
- [x] [Review][Defer] access/refresh token 共用同一 SECRET_KEY [erp-backend/app/core/security.py] — deferred，分离密钥为安全增强，后续 Story 规划
- [x] [Review][Defer] seed.py 将明文密码打印到 stdout [erp-backend/app/db/seed.py] — deferred，仅限 dev 工具，可接受
- [x] [Review][Defer] 每次请求均查询 DB 获取用户，无缓存 [erp-backend/app/deps.py] — deferred，性能优化，超出本 Story 范围
- [x] [Review][Defer] updated_at 无自动更新触发器（BaseModel 问题）[erp-backend/alembic/versions/0001_create_users_table.py] — deferred，pre-existing，BaseModel 遗留问题
- [x] [Review][Defer] SECRET_KEY 默认值弱校验依赖 Story 1.1 的验证器 — deferred，pre-existing
- [x] [Review][Defer] delete_cookie 未指定 path/domain 参数，子路径代理场景可能无法清除 — deferred，当前部署无此问题
- [x] [Review][Defer] 登录接口无速率限制/账号锁定 [erp-backend/app/routers/auth.py] — deferred，需基础设施，超出范围
- [x] [Review][Defer] is_active server_default=sa.text("1") 对 PostgreSQL 不兼容 — deferred，当前目标为 MySQL
