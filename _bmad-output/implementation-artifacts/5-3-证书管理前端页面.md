# Story 5.3: 证书管理前端页面

**Status:** review
**Story Key:** 5-3-证书管理前端页面
**Epic:** 5 - 产品证书管理
**Date:** 2026-04-20

---

## User Story

As a 产品部用户,
I want 在证书管理页面查看、筛选、新增和编辑证书，通过颜色标签一眼识别证书状态,
So that 我可以高效管理合规证书并及时处理到期风险。

---

## Acceptance Criteria

**Given** 用户进入证书列表页  
**When** 页面加载完成  
**Then** 筛选区：证书类型、归属类型、有效状态（有效/即将过期/已过期）、关键词  
**And** 表格列：证书名称、证书编号、证书类型、归属范围摘要、有效期、状态（颜色标签）  
**And** 状态标签：有效（绿 #52c41a）、即将过期（橙 #faad14）、已过期（红 #ff4d4f），同时显示文字（UX-DR16）

**Given** 用户筛选"即将过期"  
**When** 点击查询  
**Then** 仅显示即将过期的证书，便于集中处理续期

**Given** 用户点击"新增"进入证书表单页  
**When** 选择归属类型  
**Then** Radio 按钮组切换（通用/SPU归属/按分类），切换时清空已选关联对象  
**And** SPU 归属时显示 SPU 多选搜索框  
**And** 按分类时显示分类级联选择器

**Given** 用户上传证书文件  
**When** 选择文件  
**Then** 支持 PDF/JPG/PNG 格式，显示文件名和大小

---

## Scope

### In Scope

- 将 `CertificateListPage` 从占位页升级为真实证书列表页
- 新增证书前端 API 封装与类型定义
- 对接 5.1 已完成的 `GET/POST/PATCH/DELETE /api/v1/certificates`
- 实现证书列表页筛选、表格、状态标签、分页与操作栏
- 实现证书新增/编辑表单页，含基础资料、归属信息、证书文件上传
- 增加证书新增/编辑路由，并在保存成功后返回列表并刷新
- 补充前端测试，覆盖列表筛选、状态标签、归属切换、上传限制与保存行为

### Out of Scope

- 证书到期定时预警与后台扫描 → Story 5.2（已延期）
- SKU 详情页证书聚合展示深化
- 站内消息、邮件、企业微信等通知系统
- 证书枚举管理后台
- 独立证书详情只读页的复杂增强交互

---

## 依赖与前置条件

| Story | 状态 | 说明 |
|------|------|------|
| 1-4 | done | 页签导航、KeepAlive、菜单路由已可复用 |
| 1-5 | done | `FilterCard`、`FormSectionCard`、`FixedActionBar`、`PaginationBar` 已可复用 |
| 3-3 | done | SPU 表单布局与三列表单节奏已收敛 |
| 4-4 | done | 列表页前端模式已沉淀，可直接复用 |
| 4-5 | review | 新增/编辑表单页模式、上传底座与关闭页签逻辑已可参考 |
| 5-1 | done（已合并） | 证书 CRUD API、列表筛选、归属摘要与状态字段已可用 |

---

## 实施任务建议

- [x] Task 1: 证书前端 API 与类型
  - [x] 新增 `erp-frontend/src/api/certificates.ts`
  - [x] 在 `erp-frontend/src/types/product.ts` 增补证书列表、详情、表单、筛选相关类型
  - [x] 调整上传工具，支持证书文件上传目录，不与 SKU 图片目录混用

- [x] Task 2: 证书列表页主体结构
  - [x] 重写 `erp-frontend/src/features/products/certificates/pages/CertificateListPage.tsx`
  - [x] 使用 `FilterCard + 操作栏 + Table + PaginationBar` 结构
  - [x] 实现证书类型、归属类型、有效状态、关键词筛选
  - [x] 实现状态标签渲染与新增/编辑/删除操作

- [x] Task 3: 证书表单页与路由
  - [x] 新增 `CertificateFormPage`
  - [x] 在 `App.tsx` 增加 `/products/certificates/new` 与 `/products/certificates/:certificateId/edit` 路由
  - [x] 实现基础资料、归属信息两大分区与三列表单布局
  - [x] 实现归属类型切换时清空已选关联对象
  - [x] 实现 SPU 归属多选、分类级联与文件上传

- [x] Task 4: 保存与页面联动
  - [x] 新增态调用 `POST /api/v1/certificates`
  - [x] 编辑态调用 `PATCH /api/v1/certificates/{id}`
  - [x] 删除调用 `DELETE /api/v1/certificates/{id}`
  - [x] 保存成功后提示、关闭当前页签并刷新证书列表页

- [x] Task 5: 测试与验证
  - [x] 新增证书列表页测试
  - [x] 新增证书表单页测试
  - [x] 跑通前端测试与构建

---

## Dev Notes

### 现有代码基础

- 当前占位页：`erp-frontend/src/features/products/certificates/pages/CertificateListPage.tsx`
- 5.1 已提供：
  - `GET /api/v1/certificates`
  - `GET /api/v1/certificates/{id}`
  - `POST /api/v1/certificates`
  - `PATCH /api/v1/certificates/{id}`
  - `DELETE /api/v1/certificates/{id}`
- 现有前端模式优先参考：
  - `erp-frontend/src/features/products/skus/pages/SKUListPage.tsx`
  - `erp-frontend/src/features/products/skus/pages/SKUFormPage.tsx`
  - `erp-frontend/src/features/products/spus/pages/SPUFormPage.tsx`
  - `erp-frontend/src/utils/upload.ts`

### 关键实现约束

1. 列表页遵循当前仓库已收敛的紧凑内容区，不额外展示页面标题
2. 所有数据请求必须走 TanStack Query / mutation，不要在组件内直接调用 axios
3. 新增/编辑页必须遵循“三列表单 + 独立卡片分区 + 卡片间距 16px + 卡片内紧凑节奏”
4. 归属类型切换时必须清空无关选择值，避免提交脏数据
5. 没有真实 SPU 远程搜索接口时，不要伪装成远程搜索；可基于现有列表接口实现可搜索多选
6. 文件上传只做单文件，限制 PDF/JPG/PNG，显示文件名和大小
7. 证书状态颜色和文案必须稳定：有效=绿、即将过期=橙、已过期=红
8. Story 5.2 已延期，5.3 直接复用 5.1 的动态 `validity_status`，不要在前端臆造新的状态来源

### 推荐类型与字段

- `CertificateListItem`
  - `id`
  - `name`
  - `certificate_no`
  - `certificate_type`
  - `issuing_authority`
  - `valid_from`
  - `valid_to`
  - `ownership_type`
  - `ownership_summary`
  - `validity_status`
  - `spu_ids`
  - `category_ids`
  - `created_at`
- `CertificateDetail`
  - 以上字段 + `spus`、`categories`、`file_*`、`remarks`
- `CertificateMutationPayload`
  - `name`
  - `certificate_no`
  - `certificate_type`
  - `issuing_authority`
  - `valid_from`
  - `valid_to`
  - `ownership_type`
  - `spu_ids`
  - `category_ids`
  - `file_object_key`
  - `file_url`
  - `file_name`
  - `remarks`

### 测试重点

- 页面首次加载触发证书列表查询
- 设置归属类型、有效状态、关键词后点击“查询”会带正确参数重新查询
- 状态标签按有效/即将过期/已过期渲染不同颜色和文字
- 产品部/管理员可见“新增 / 编辑 / 删除”，其他角色仅保留只读入口
- 归属类型切换时会清空已选 SPU / 分类
- 表单保存成功后关闭页签并刷新列表
- 文件上传限制格式并展示文件名

### References

- `_bmad-output/planning-artifacts/epics.md`（Story 5.3）
- `_bmad-output/planning-artifacts/prd-product-management.md`（证书列表页 / 新增编辑页）
- `_bmad-output/planning-artifacts/ux-design-specification.md`（旅程五、状态标签、文件上传）
- `_bmad-output/implementation-artifacts/4-4-sku-列表页与筛选.md`
- `_bmad-output/implementation-artifacts/4-5-sku-新增编辑表单页.md`
- `_bmad-output/implementation-artifacts/5-1-证书数据模型与-crud-api.md`
- `_bmad-output/implementation-artifacts/deferred-work.md`

---

## Dev Agent Record

### Debug Log

- 2026-04-20: create-story 完成，已基于 Epic 5、PRD、UX、4.4/4.5 前端模式与 5.1 证书后端契约生成 5.3 开发上下文
- 2026-04-20: 新增 `certificatesApi`、证书前端类型定义，并将上传工具扩展为支持自定义文件目录
- 2026-04-20: 将 `CertificateListPage` 从占位页升级为真实列表页，完成筛选区、状态标签、分页、新增/编辑/删除操作
- 2026-04-20: 新增 `CertificateFormPage`，完成基础资料、归属信息、单文件上传，以及新增/编辑保存链路
- 2026-04-20: 在 `App.tsx` 中补齐证书新增/编辑路由，沿用 KeepAlive 与 route wrapper 模式
- 2026-04-20: 新增证书列表页与表单页测试；前端验证通过，`bash scripts/frontend-test.sh` 75/75 通过
- 2026-04-20: 构建验证通过，`cd erp-frontend && npm run build` 成功（存在 Vite 大包体 warning，但不影响本 Story 交付）
- 2026-04-21: 根据 code review 补齐证书只读详情页、查看入口和证书 `new / detail / edit` 页签标题恢复映射
- 2026-04-21: 根据 code review 修复证书删除后详情缓存未失效问题
- 2026-04-21: 根据 code review 将证书文件保存链路调整为“先保存证书，再上传文件并回写”，新增失败时回滚删除证书，编辑替换成功后清理旧文件，旧文件清理失败只记录日志不回滚新文件
- 2026-04-21: 为证书文件补偿清理新增 `/api/v1/files/object` 删除接口，并扩展后端存储删除逻辑支持通用文件桶
- 2026-04-21: 修复证书表单在未挂载归属字段缺失时点击保存前端直接崩溃的问题（`category_paths/spu_ids` 统一兜底为 `[]`）
- 2026-04-21: review 修复后再次验证通过，`bash scripts/frontend-test.sh` 84/84 通过，`bash scripts/backend-test.sh` 93/93 通过，`cd erp-frontend && npm run build` 成功

### Completion Notes

- 已生成 5.3 故事上下文，供 `dev-story` 直接实施
- 已完成证书列表页真实实现，支持证书类型、归属类型、有效状态、关键词筛选
- 已完成证书新增/编辑表单页，支持归属切换清空、SPU 多选、分类级联和单文件上传
- 已补齐证书前端 API、路由与上传目录适配，直接复用 5.1 的后端契约
- 当前实现直接复用 5.1 的动态 `validity_status`，未越界实现 5.2 的定时预警
- 已完成证书只读详情页、查看入口和动态页签标题恢复，满足 Story 5.3 的“查看 / 筛选 / 新增 / 编辑”闭环
- 已补齐证书上传补偿清理链路，避免新增失败残留半成功证书、替换文件残留旧对象、删除旧对象失败误删新文件
- Story 5.3 当前可进入 code review

### Review Findings

- [x] [Review][Patch] 证书列表缺少查看入口，读权限角色无法查看详情
- [x] [Review][Patch] 新增/编辑证书页刷新后不会恢复正确页签标题
- [x] [Review][Patch] 删除后没有同步失效证书详情缓存
- [x] [Review][Patch] 上传先于保存会在失败时留下孤儿文件
- [x] [Review][Patch] 创建态文件回写失败后会留下一条“无文件”的真实证书记录
- [x] [Review][Patch] 编辑证书替换文件时旧对象不会被清理
- [x] [Review][Patch] 删除旧文件失败时会误删新文件并留下坏的证书引用

### File List

- `_bmad-output/implementation-artifacts/5-3-证书管理前端页面.md`
- `_bmad-output/implementation-artifacts/sprint-status.yaml`
- `_bmad-output/implementation-artifacts/deferred-work.md`
- `erp-backend/app/core/storage.py`
- `erp-backend/app/routers/files.py`
- `erp-backend/app/services/files.py`
- `erp-backend/tests/routers/test_files.py`
- `erp-frontend/src/App.tsx`
- `erp-frontend/src/api/certificates.ts`
- `erp-frontend/src/api/files.ts`
- `erp-frontend/src/components/layout/SideMenu.tsx`
- `erp-frontend/src/features/products/certificates/pages/CertificateDetailPage.tsx`
- `erp-frontend/src/features/products/certificates/pages/CertificateFormPage.tsx`
- `erp-frontend/src/features/products/certificates/pages/CertificateListPage.tsx`
- `erp-frontend/src/test/components/SideMenu.test.tsx`
- `erp-frontend/src/test/features/CertificateFormPage.test.tsx`
- `erp-frontend/src/test/features/CertificateListPage.test.tsx`
- `erp-frontend/src/types/product.ts`
- `erp-frontend/src/utils/upload.ts`

### Change Log

- 2026-04-20: 初始创建 Story 5.3 implementation artifact
- 2026-04-20: 完成证书管理前端页面、测试与构建验证，并将 Story 状态更新为 `review`
- 2026-04-21: 根据多轮 code review 补齐查看入口、详情页、缓存失效、文件补偿清理与前端保存异常修复
