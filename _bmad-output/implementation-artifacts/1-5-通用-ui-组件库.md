# Story 1.5: 通用 UI 组件库

**Status:** ready-for-dev
**Story Key:** 1-5-通用-ui-组件库
**Epic:** 1 - 系统初始化与用户认证
**Date:** 2026-04-15

---

## User Story

As a 开发人员,
I want 一套可复用的自定义 UI 组件（SectionTitle、FixedActionBar、FilterCard、InheritedField、PaginationBar），
So that 后续所有子模块的列表页和表单页可以保持统一的视觉规范和交互模式。

---

## Acceptance Criteria

**Given** SectionTitle 组件
**When** 在表单页使用
**Then** 显示左侧 3px #C41D2E 红色竖线 + 红色 16px 标题文字
**And** 具有 `role="heading"` `aria-level="3"` 无障碍属性

**Given** FixedActionBar 组件
**When** 在表单页底部使用
**Then** 固定在页面底部，高度 56px，白色背景，顶部 1px 分隔线 + 阴影
**And** 保存/取消按钮居中对齐
**And** 保存按钮支持 loading 态

**Given** FilterCard 组件
**When** 在列表页筛选区使用
**Then** 白色背景卡片，圆角 4px，1px #f0f0f0 边框，内边距 16px 16px 0 16px

**Given** InheritedField 组件
**When** 展示从 SPU 继承的只读字段
**Then** 灰色背景 #fafafa + "继承自 SPU" 灰色小标签
**And** 具有 `aria-readonly="true"` 属性

**Given** PaginationBar 组件
**When** 在列表页表格下方使用
**Then** 左侧显示"共 {total} 条"，右侧显示 AntD Pagination 控件
**And** 高度 48px，上边距 12px

---

## Scope

### In Scope

- `SectionTitle` 组件（UX-DR5）
- `FixedActionBar` 组件（UX-DR6）
- `FilterCard` 组件（UX-DR7）
- `InheritedField` 组件（UX-DR8）
- `PaginationBar` 组件（UX-DR9）
- 每个组件的 Vitest 单元测试
- 各组件的 TypeScript 类型定义（Props interface）

### Out of Scope（不要实现）

- 实际业务列表页/表单页（使用这些组件的页面） → Story 2.x+
- `RemoteSearchSelect` 封装（UX-DR14） → 在实际需要它的 Story 中实现
- 证书状态标签组件（UX-DR16） → Story 5.3
- 价格审批状态标签（UX-DR15） → Story 7.3
- 后端 API → 不涉及

---

## 前置依赖

| Story | 状态 | 提供什么 |
|-------|------|----------|
| Story 1.1 | done | 前端项目骨架、Vite + React 19 + TypeScript 严格模式配置 |
| Story 1.4 | done | Tailwind CSS `important: '#root'`、AntD ConfigProvider 主题（`#C41D2E`、borderRadius 4）、feature-based 目录结构 |

**注意：** 本 Story 纯前端，无 API 依赖，无需后端服务运行。

---

## 开发环境

| 变量 | 值 |
|------|-----|
| FRONTEND_ROOT | `erp-frontend/` |
| 前端启动 | `cd erp-frontend && npm run dev`（端口 5173） |
| 测试执行 | `cd erp-frontend && npx vitest run` |

---

## 新增依赖

本 Story **无需安装新依赖**。所有依赖（`antd`、`@ant-design/icons`、`react`、`tailwindcss`）已在 Story 1.1/1.4 中安装。

---

## 文件清单

### 新增文件

| 文件路径 | 说明 |
|----------|------|
| `src/components/common/SectionTitle.tsx` | 分区标题组件 |
| `src/components/common/FixedActionBar.tsx` | 表单页底部固定操作栏 |
| `src/components/common/FilterCard.tsx` | 列表页筛选区容器卡片 |
| `src/components/common/InheritedField.tsx` | SPU 继承字段只读展示 |
| `src/components/common/PaginationBar.tsx` | 列表页分页栏 |
| `src/components/common/index.ts` | 统一导出入口 |
| `src/test/components/SectionTitle.test.tsx` | SectionTitle 单元测试 |
| `src/test/components/FixedActionBar.test.tsx` | FixedActionBar 单元测试 |
| `src/test/components/FilterCard.test.tsx` | FilterCard 单元测试 |
| `src/test/components/InheritedField.test.tsx` | InheritedField 单元测试 |
| `src/test/components/PaginationBar.test.tsx` | PaginationBar 单元测试 |

### 不要修改的已存在文件

- `src/components/layout/AppLayout.tsx`、`SideMenu.tsx`、`CacheTabs.tsx` — Story 1.4 产出，保持原样
- `src/App.tsx` — 已完整配置 ConfigProvider 主题，不要改动

---

## 技术规范

### 组件目录位置

所有通用 UI 组件统一放在 `src/components/common/`（架构文档定义：`components/` 为通用 UI 组件目录）。

---

### 1. SectionTitle 组件（UX-DR5）

```tsx
// src/components/common/SectionTitle.tsx
interface SectionTitleProps {
  title: string
  className?: string
}

export default function SectionTitle({ title, className }: SectionTitleProps) {
  return (
    <div
      role="heading"
      aria-level={3}
      className={className}
      style={{
        display: 'flex',
        alignItems: 'center',
        gap: 8,
        marginBottom: 16,
      }}
    >
      {/* 左侧 3px #C41D2E 红色竖线 */}
      <div
        style={{
          width: 3,
          height: 16,
          backgroundColor: '#C41D2E',
          borderRadius: 1,
          flexShrink: 0,
        }}
      />
      {/* 红色 16px 标题文字 */}
      <span
        style={{
          fontSize: 16,
          fontWeight: 600,
          color: '#C41D2E',
          lineHeight: '16px',
        }}
      >
        {title}
      </span>
    </div>
  )
}
```

**使用示例（后续 Story 参考）：**

```tsx
<SectionTitle title="基础信息" />
<SectionTitle title="采购信息" />
```

---

### 2. FixedActionBar 组件（UX-DR6）

```tsx
// src/components/common/FixedActionBar.tsx
import { Button, Space } from 'antd'

interface FixedActionBarProps {
  onSave: () => void
  onCancel: () => void
  loading?: boolean
  saveText?: string
  cancelText?: string
}

export default function FixedActionBar({
  onSave,
  onCancel,
  loading = false,
  saveText = '保存',
  cancelText = '取消',
}: FixedActionBarProps) {
  return (
    <div
      style={{
        position: 'fixed',
        bottom: 0,
        left: 0,
        right: 0,
        height: 56,
        backgroundColor: '#fff',
        borderTop: '1px solid #f0f0f0',
        boxShadow: '0 -2px 8px rgba(0, 0, 0, 0.06)',
        display: 'flex',
        alignItems: 'center',
        justifyContent: 'center',
        zIndex: 100,
      }}
    >
      <Space size={12}>
        <Button onClick={onCancel} disabled={loading}>
          {cancelText}
        </Button>
        <Button type="primary" onClick={onSave} loading={loading}>
          {saveText}
        </Button>
      </Space>
    </div>
  )
}
```

**关键说明：**
- `position: fixed` 固定在视口底部，不跟随页面滚动
- `zIndex: 100` 确保在表格、下拉等元素上方
- 表单页底部需预留 56px padding，防止内容被遮挡：`<div style={{ paddingBottom: 56 }}>`
- `loading` 为 true 时，取消按钮 `disabled`，防止重复提交

---

### 3. FilterCard 组件（UX-DR7）

```tsx
// src/components/common/FilterCard.tsx
import type { ReactNode } from 'react'

interface FilterCardProps {
  children: ReactNode
  className?: string
}

export default function FilterCard({ children, className }: FilterCardProps) {
  return (
    <div
      className={className}
      style={{
        backgroundColor: '#fff',
        borderRadius: 4,
        border: '1px solid #f0f0f0',
        padding: '16px 16px 0 16px',
        marginBottom: 16,
      }}
    >
      {children}
    </div>
  )
}
```

**使用示例（后续 Story 参考）：**

```tsx
<FilterCard>
  <Form layout="inline">
    <Form.Item label="关键词">
      <Input placeholder="SKU编码/名称" />
    </Form.Item>
    <Form.Item>
      <Button type="primary">查询</Button>
      <Button style={{ marginLeft: 8 }}>重置</Button>
    </Form.Item>
  </Form>
</FilterCard>
```

**注意：**
- `padding: '16px 16px 0 16px'`（上右下左），底部为 0 是因为 AntD Form.Item 自带下方 margin
- 不要改变这个内边距规范，后续列表页必须保持一致

---

### 4. InheritedField 组件（UX-DR8）

```tsx
// src/components/common/InheritedField.tsx
import type { ReactNode } from 'react'

interface InheritedFieldProps {
  value: ReactNode
  sourceLabel?: string
  className?: string
}

export default function InheritedField({
  value,
  sourceLabel = '继承自 SPU',
  className,
}: InheritedFieldProps) {
  return (
    <div
      aria-readonly="true"
      className={className}
      style={{
        display: 'flex',
        alignItems: 'center',
        gap: 8,
        minHeight: 32,
        padding: '4px 8px',
        backgroundColor: '#fafafa',
        border: '1px solid #d9d9d9',
        borderRadius: 4,
      }}
    >
      {/* 继承值 */}
      <span style={{ flex: 1, color: 'rgba(0,0,0,0.85)', fontSize: 14 }}>
        {value ?? '—'}
      </span>
      {/* "继承自 SPU" 灰色小标签 */}
      <span
        style={{
          fontSize: 11,
          color: 'rgba(0,0,0,0.35)',
          backgroundColor: '#f0f0f0',
          padding: '1px 6px',
          borderRadius: 2,
          whiteSpace: 'nowrap',
          flexShrink: 0,
        }}
      >
        {sourceLabel}
      </span>
    </div>
  )
}
```

**使用示例（后续 Story 参考）：**

```tsx
// SKU 表单中继承字段（UX-DR10）
<Form.Item label="所属分类">
  <InheritedField value="医疗器械 / 超声设备 / 探头" />
</Form.Item>

<Form.Item label="供应商">
  <InheritedField value="深圳明达医疗" />
</Form.Item>
```

**关键说明：**
- `aria-readonly="true"` 是 Accessibility 合规要求
- `value` 为空时显示 `—` 占位符，避免空白
- `sourceLabel` 支持自定义，便于未来支持其他继承来源（如"继承自分类"）

---

### 5. PaginationBar 组件（UX-DR9）

```tsx
// src/components/common/PaginationBar.tsx
import { Pagination } from 'antd'

interface PaginationBarProps {
  total: number
  current: number
  pageSize?: number
  onChange: (page: number, pageSize: number) => void
  className?: string
}

export default function PaginationBar({
  total,
  current,
  pageSize = 20,
  onChange,
  className,
}: PaginationBarProps) {
  return (
    <div
      className={className}
      style={{
        display: 'flex',
        alignItems: 'center',
        justifyContent: 'space-between',
        height: 48,
        marginTop: 12,
        padding: '0 4px',
      }}
    >
      {/* 左侧：共 X 条 */}
      <span style={{ color: 'rgba(0,0,0,0.45)', fontSize: 14 }}>
        共 <strong style={{ color: 'rgba(0,0,0,0.85)' }}>{total}</strong> 条
      </span>
      {/* 右侧：AntD Pagination */}
      <Pagination
        current={current}
        total={total}
        pageSize={pageSize}
        onChange={onChange}
        showSizeChanger
        showQuickJumper
        pageSizeOptions={[10, 20, 50, 100]}
      />
    </div>
  )
}
```

**使用示例（后续 Story 参考）：**

```tsx
// 列表页三段式布局（UX-DR13）
<FilterCard>...</FilterCard>
<div style={{ marginBottom: 12, display: 'flex', justifyContent: 'space-between' }}>
  <Button type="primary" icon={<PlusOutlined />}>新增</Button>
</div>
<Table dataSource={data} columns={columns} pagination={false} size="middle" />
<PaginationBar
  total={total}
  current={page}
  pageSize={pageSize}
  onChange={(page, size) => {
    setPage(page)
    setPageSize(size)
  }}
/>
```

**注意：**
- AntD Table 的 `pagination` 必须设为 `false`，分页由 PaginationBar 单独控制
- `showSizeChanger` + `showQuickJumper` 对于 10,000+ 条 SKU 数据是必须的

---

### 6. 统一导出（index.ts）

```typescript
// src/components/common/index.ts
export { default as SectionTitle } from './SectionTitle'
export { default as FixedActionBar } from './FixedActionBar'
export { default as FilterCard } from './FilterCard'
export { default as InheritedField } from './InheritedField'
export { default as PaginationBar } from './PaginationBar'
```

**后续 Story 导入方式：**

```typescript
import { SectionTitle, FixedActionBar, FilterCard, InheritedField, PaginationBar } from '@/components/common'
// 或
import { SectionTitle } from '../../components/common'
```

---

## 测试规范

使用 Vitest + React Testing Library（已在 Story 1.1 配置好）。

### SectionTitle 测试

```tsx
// src/test/components/SectionTitle.test.tsx
import { render, screen } from '@testing-library/react'
import { describe, it, expect } from 'vitest'
import SectionTitle from '../../components/common/SectionTitle'

describe('SectionTitle', () => {
  it('渲染标题文字', () => {
    render(<SectionTitle title="基础信息" />)
    expect(screen.getByText('基础信息')).toBeInTheDocument()
  })

  it('具有正确的无障碍属性', () => {
    render(<SectionTitle title="采购信息" />)
    const heading = screen.getByRole('heading')
    expect(heading).toHaveAttribute('aria-level', '3')
  })
})
```

### FixedActionBar 测试

```tsx
// src/test/components/FixedActionBar.test.tsx
import { render, screen, fireEvent } from '@testing-library/react'
import { describe, it, expect, vi } from 'vitest'
import FixedActionBar from '../../components/common/FixedActionBar'

describe('FixedActionBar', () => {
  it('渲染保存和取消按钮', () => {
    render(<FixedActionBar onSave={vi.fn()} onCancel={vi.fn()} />)
    expect(screen.getByText('保存')).toBeInTheDocument()
    expect(screen.getByText('取消')).toBeInTheDocument()
  })

  it('点击保存触发 onSave 回调', () => {
    const onSave = vi.fn()
    render(<FixedActionBar onSave={onSave} onCancel={vi.fn()} />)
    fireEvent.click(screen.getByText('保存'))
    expect(onSave).toHaveBeenCalledTimes(1)
  })

  it('loading 状态下取消按钮禁用', () => {
    render(<FixedActionBar onSave={vi.fn()} onCancel={vi.fn()} loading={true} />)
    const cancelButton = screen.getByText('取消').closest('button')
    expect(cancelButton).toBeDisabled()
  })

  it('支持自定义按钮文字', () => {
    render(<FixedActionBar onSave={vi.fn()} onCancel={vi.fn()} saveText="提交" cancelText="返回" />)
    expect(screen.getByText('提交')).toBeInTheDocument()
    expect(screen.getByText('返回')).toBeInTheDocument()
  })
})
```

### FilterCard 测试

```tsx
// src/test/components/FilterCard.test.tsx
import { render, screen } from '@testing-library/react'
import { describe, it, expect } from 'vitest'
import FilterCard from '../../components/common/FilterCard'

describe('FilterCard', () => {
  it('渲染子内容', () => {
    render(<FilterCard><span>筛选内容</span></FilterCard>)
    expect(screen.getByText('筛选内容')).toBeInTheDocument()
  })

  it('应用正确的边距样式', () => {
    const { container } = render(<FilterCard><span>内容</span></FilterCard>)
    const card = container.firstChild as HTMLElement
    expect(card.style.backgroundColor).toBe('rgb(255, 255, 255)')
    expect(card.style.borderRadius).toBe('4px')
  })
})
```

### InheritedField 测试

```tsx
// src/test/components/InheritedField.test.tsx
import { render, screen } from '@testing-library/react'
import { describe, it, expect } from 'vitest'
import InheritedField from '../../components/common/InheritedField'

describe('InheritedField', () => {
  it('渲染传入的值', () => {
    render(<InheritedField value="深圳明达医疗" />)
    expect(screen.getByText('深圳明达医疗')).toBeInTheDocument()
  })

  it('显示默认"继承自 SPU"标签', () => {
    render(<InheritedField value="某供应商" />)
    expect(screen.getByText('继承自 SPU')).toBeInTheDocument()
  })

  it('具有 aria-readonly 属性', () => {
    const { container } = render(<InheritedField value="测试" />)
    const el = container.firstChild as HTMLElement
    expect(el).toHaveAttribute('aria-readonly', 'true')
  })

  it('value 为空时显示占位符 —', () => {
    render(<InheritedField value={null} />)
    expect(screen.getByText('—')).toBeInTheDocument()
  })

  it('支持自定义 sourceLabel', () => {
    render(<InheritedField value="医疗器械" sourceLabel="继承自分类" />)
    expect(screen.getByText('继承自分类')).toBeInTheDocument()
  })
})
```

### PaginationBar 测试

```tsx
// src/test/components/PaginationBar.test.tsx
import { render, screen } from '@testing-library/react'
import { describe, it, expect, vi } from 'vitest'
import PaginationBar from '../../components/common/PaginationBar'

describe('PaginationBar', () => {
  it('显示正确的总条数', () => {
    render(<PaginationBar total={234} current={1} onChange={vi.fn()} />)
    expect(screen.getByText('234')).toBeInTheDocument()
    // 验证文字包含"共 X 条"
    expect(screen.getByText(/共/).textContent).toContain('234')
  })

  it('total 为 0 时显示"共 0 条"', () => {
    render(<PaginationBar total={0} current={1} onChange={vi.fn()} />)
    expect(screen.getByText('0')).toBeInTheDocument()
  })
})
```

---

## 架构合规清单

| 规则 | 验证点 |
|------|--------|
| 通用 UI 组件放 `src/components/` | 所有 5 个组件在 `src/components/common/` |
| TypeScript 严格模式，无 `any` | 所有 Props 有明确类型，无 `any` |
| 不使用 CSS 模块或 styled-components | 使用 inline style（与已建立的模式一致） |
| AntD 主题色 `#C41D2E` 通过 ConfigProvider 全局生效 | 组件中不引入 AntD ConfigProvider，颜色直接用 `#C41D2E` 常量 |
| Tailwind `important: '#root'` 已配置 | 本 Story 使用 inline style 为主，Tailwind utility class 可辅助 |
| 不在本 Story 实现业务逻辑 | 只实现通用展示组件，零业务逻辑 |

---

## 前置故事智能（Story 1.4 经验）

**必须遵守，避免踩坑：**

| 从 Story 1.4 学到的教训 | 在本 Story 中的影响 |
|------------------------|---------------------|
| `react-activation@0.13.4` 已安装，React 19 兼容 | 本 Story 无额外依赖，无影响 |
| `src/components/layout/` 已有 AppLayout / SideMenu / CacheTabs | 本 Story 使用 `src/components/common/`，不要改 layout/ |
| AntD `Tabs editable-card` 类型不渲染 inkBar，需 CSS override | 本 Story 不涉及 Tabs，无影响 |
| `uiStore.ts` 导出名为 `useUIStore`（大写 UI） | 本 Story 组件不依赖 uiStore，无影响 |
| `src/test/` 目录已有测试文件（`App.test.tsx` 等） | 新增测试放 `src/test/components/` 子目录 |
| 使用 `npx vitest run` 执行测试 | 本 Story 测试同样用此命令验证 |
| FixedActionBar 的 `position: fixed` 会遮盖页面底部内容 | 表单容器需 `paddingBottom: 56`（已在规范中说明） |

---

## 完成标准

- [ ] `SectionTitle` 渲染正确：3px 红竖线 + 红色 16px 文字 + `role="heading"` `aria-level="3"`
- [ ] `FixedActionBar` 渲染正确：固定底部 56px 高 + 白色背景 + 顶部分隔线 + 按钮居中 + loading 态
- [ ] `FilterCard` 渲染正确：白色背景 + 4px 圆角 + `#f0f0f0` 边框 + 16px 16px 0 16px 内边距
- [ ] `InheritedField` 渲染正确：`#fafafa` 背景 + "继承自 SPU" 小标签 + `aria-readonly="true"`
- [ ] `PaginationBar` 渲染正确：左侧"共 X 条" + 右侧 AntD Pagination + 高度 48px + 上边距 12px
- [ ] `src/components/common/index.ts` 统一导出所有 5 个组件
- [ ] 所有 5 个组件有对应测试文件，测试通过
- [ ] `npx vitest run` 全部测试通过（含已有测试，不回归）
- [ ] `npm run build` 无 TypeScript 错误

---

## Dev Agent Record

### Change Log

- 2026-04-15: Story 创建，状态 ready-for-dev
- 2026-04-15: Story 实现完成，状态 done
  - 新增 5 个通用 UI 组件：SectionTitle、FixedActionBar、FilterCard、InheritedField、PaginationBar
  - 新增 index.ts 统一导出入口
  - 新增 5 个组件的 Vitest 单元测试，全部 30 个测试通过
  - 修复 vitest.config.ts 中 NODE_ENV 配置，解决 React 19 act 兼容性问题
  - 分支：story/1-5-common-ui-component-library
