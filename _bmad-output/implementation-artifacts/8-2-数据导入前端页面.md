# Story 8.2: 数据导入前端页面

**Status:** review
**Story Key:** 8-2-数据导入前端页面
**Epic:** 8 - 数据导入与系统配置
**Date:** 2026-04-24

---

## User Story

As a 管理员,
I want 在导入页面选择导入类型、下载模板、上传文件、查看校验结果并确认导入,
So that 数据迁移过程可视化、可控。

---

## Acceptance Criteria

**Given** 用户进入数据导入页面
**When** 页面加载完成
**Then** 显示三个导入类型选项卡（分类 / SPU / SKU）
**And** 每个类型提供“下载模板”按钮和“上传 Excel”按钮

**Given** 用户上传 Excel 文件
**When** 校验完成
**Then** 页面展示校验结果：成功 X 条 / 失败 Y 条
**And** 失败记录以表格展示（行号、字段名、错误原因）
**And** 全部成功时显示“确认导入”按钮

**Given** 导入耗时较长
**When** 处理进行中
**Then** 显示进度条或百分比反馈

**Given** 非管理员 / 非产品部用户
**When** 访问数据导入页面
**Then** 页面不可见或提示无权限

---

## Scope

### In Scope

- 数据导入页从占位页升级为真实页面
- 分类 / SPU / SKU 三类导入 Tab 与统一页面框架
- 模板下载、文件上传、校验结果展示、确认导入与进度查询
- 前端 API 封装、类型定义、页面状态管理与基础测试
- 管理员 / 产品部前端访问权限收口

### Out of Scope

- 8.1 后端导入能力再扩展
- 导入历史列表、失败重试中心、异步任务后台
- Excel 模板字段新增或后端契约重设计
- Epic 8 之外的系统配置页面改造

---

## 依赖与前置条件

| Story | 状态 | 说明 |
|------|------|------|
| 1-3 | done | 角色与权限能力可复用 |
| 1-4 | done | 应用骨架、路由、KeepAlive 与菜单模式已收敛 |
| 1-5 | done | `FilterCard`、`FormSectionCard`、`EmptyState` 等通用组件可复用 |
| 8-1 | done | 模板下载、上传校验、确认导入、进度查询 API 已合并到 `main` |

---

## 实施任务建议

- [x] Task 1: 梳理 8.1 API 契约与页面交互流
  - [x] 明确三类导入 Tab 的展示文案、接口映射与结果结构
  - [x] 明确页面状态流：初始态 / 上传校验完成 / 可确认 / 导入中 / 导入完成 / 无权限
  - [x] 明确错误表格字段与空态策略

- [x] Task 2: 前端基础设施
  - [x] 新增导入 API 封装与类型定义
  - [x] 补充文件上传、模板下载、轮询进度的请求层能力
  - [x] 收口权限判断与页面访问提示

- [x] Task 3: 页面实现
  - [x] 将 `ImportPage` 从占位页升级为真实导入工作台
  - [x] 实现分类 / SPU / SKU 三个 Tab 的统一操作区
  - [x] 实现校验结果摘要、失败记录表格、确认导入按钮与进度展示
  - [x] 处理重复上传、切换 Tab、重新导入后的状态重置

- [x] Task 4: 测试与验证
  - [x] 补充导入页面核心测试
  - [x] 跑通前端测试与构建验证
  - [x] 联调 8.1 API 的关键流程

---

## Dev Notes

### 当前已知上下文

1. 8.1 已提供 `templates`、`validate`、`confirm`、`tasks/{id}` 四类接口，8.2 只消费现有契约，不应反向扩展后端。
2. 当前页面入口 `erp-frontend/src/features/products/import/pages/ImportPage.tsx` 仍是简单占位实现。
3. 前端约定要求列表 / 工作台页面保持紧凑内容区，不重复叠加页面标题和大块空白。
4. 访问权限验收口径是“管理员 / 产品部可见，其他角色不可见或明确提示无权限”，实现时需和现有权限工具保持一致。
5. 导入结果表格至少需要稳定展示：行号、字段名、错误原因，并支持切换导入类型后清晰重置状态。

### 初步实现约束

1. 页面优先采用单页三 Tab 工作台，不拆成多个子路由。
2. 上传成功后的状态需要绑定当前 `task_id`，确认导入和进度轮询都基于该任务继续。
3. 只有 `failed_count == 0` 且后端返回可确认时，页面才显示“确认导入”动作。
4. 导入进行中必须避免用户重复点击确认，且轮询结束后需要收口到成功或失败状态。
5. 文件上传仅接受 `.xlsx`，错误提示优先直接复用后端返回消息。

### References

- `_bmad-output/planning-artifacts/epics.md`（Story 8.2）
- `_bmad-output/planning-artifacts/architecture.md`
- `_bmad-output/implementation-artifacts/8-1-excel-导入模板与批量导入-api.md`
- `_bmad-output/implementation-artifacts/1-4-前端应用骨架与导航系统.md`
- `_bmad-output/implementation-artifacts/1-5-通用-ui-组件库.md`

---

## Dev Agent Record

### Debug Log

- 2026-04-24: 从 `main` 切出分支 `codex/8-2-import-page`
- 2026-04-24: 创建 Story 8.2 implementation artifact，并将 sprint 状态更新为 `in-progress`
- 2026-04-24: 新增 `erp-frontend/src/api/imports.ts` 与导入类型定义，收口模板下载、Excel 上传校验、确认导入与任务进度查询接口
- 2026-04-24: 将 `ImportPage` 从占位页升级为三 Tab 导入工作台，支持权限提示、模板下载、上传校验、失败表格、确认导入与进度轮询
- 2026-04-24: 新增 `ImportPage` 前端测试并完成前端构建与全量测试回归

### Completion Notes

- 已完成数据导入前端工作台，支持分类 / SPU / SKU 三类导入 Tab，统一提供模板下载与 Excel 上传入口
- 已新增 `importsApi` 与导入相关类型定义，前端可直接消费 8.1 的模板下载、上传校验、确认导入与任务进度接口
- 已实现校验结果摘要、失败记录表格、任务状态标签、导入进度条，以及“校验通过后才可确认导入”的页面约束
- 已实现导入中轮询任务进度，并在导入完成或失败后自动收口页面状态
- 已对无权限角色展示明确提示，权限口径与现有 `canAccessImport` 保持一致
- 已补充 `ImportPage` 页面测试，并完成前端全量测试与构建验证
- 当前残余风险：确认导入接口仍是同步请求，页面轮询虽然能展示中间进度，但大文件场景的实际交互体验还需要后续联调观察

### Verification

- `npm --prefix erp-frontend test -- --run src/test/features/ImportPage.test.tsx`
- `npm --prefix erp-frontend test`
- `npm --prefix erp-frontend run build`

### File List

- `_bmad-output/implementation-artifacts/8-2-数据导入前端页面.md`
- `_bmad-output/implementation-artifacts/sprint-status.yaml`
- `erp-frontend/src/api/imports.ts`
- `erp-frontend/src/features/products/import/pages/ImportPage.tsx`
- `erp-frontend/src/test/features/ImportPage.test.tsx`
- `erp-frontend/src/types/product.ts`
