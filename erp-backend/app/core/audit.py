# Story 1.6 将实现审计日志服务
# 本文件为占位，预留接口供后续 Story 填充


async def log_action(
    user_id: int,
    action: str,
    resource: str,
    resource_id: int | None = None,
    details: dict | None = None,
) -> None:
    """记录审计日志 — Story 1.6 将实现此方法"""
    pass  # 占位，不阻塞其他 Story 开发
